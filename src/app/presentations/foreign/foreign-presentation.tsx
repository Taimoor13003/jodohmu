"use client";

import Image from "next/image";
import { useCallback, useEffect, useMemo, useRef, useState } from "react";
import { ArrowLeft, ArrowRight, ChevronDown, Expand, Heart, MessageCircle, Settings2, ShieldCheck, Users, X } from "lucide-react";

type Package = { name: string; label: string; window: string; price: string; description: string; features: string[]; accent: string; savings?: string };
const safar: Package = { name: "Safar", label: "The Journey", window: "6-month active matching window", price: "$1,200", accent: "#d6ac57", description: "A guided journey from a clear understanding of you to a thoughtful meeting in Indonesia.", features: ["Onboarding: your story & who you're looking for", "Profile built + identity, passport & employment verified", "Up to 3 online meetings", "1 psychology assessment + 1 background check", "1 family gathering", "2 Joble meetings in Indonesia"], savings: "22% off list price" };
const amanah: Package = { name: "Amanah", label: "The Trust", window: "12-month active matching window", price: "$3,000", accent: "#d85b87", description: "A private, concierge journey with more time, deeper support, and a more considered path to meeting in Indonesia.", features: ["Deep onboarding + personality assessment", "Dedicated matchmaker, priority search", "Up to 10 online meetings", "2 psychology assessments + 2 background checks", "1 family gathering", "4 Joble meetings in Indonesia"], savings: "18% off list price" };
const whatsappHref = "https://wa.me/6281122210303?text=" + encodeURIComponent("Hello Jodohmu, I would like to discuss the international matchmaking journey.");

export function ForeignPresentation({ client, pricesVisible, showSafar, showAmanah, presenter }: { client?: string; pricesVisible: boolean; showSafar: boolean; showAmanah: boolean; presenter: boolean }) {
  const [slide, setSlide] = useState(0); const [isFullscreen, setIsFullscreen] = useState(false); const [settingsOpen, setSettingsOpen] = useState(false); const [selected, setSelected] = useState({ safar: showSafar, amanah: showAmanah }); const [showPrices, setShowPrices] = useState(pricesVisible); const [packagePrices, setPackagePrices] = useState({ safar: safar.price, amanah: amanah.price }); const [custom, setCustom] = useState<Package | null>(null); const deckRef = useRef<HTMLDivElement>(null); const touchStart = useRef<number | null>(null);
  const greeting = client ? `Prepared for ${client}` : "A private introduction";
  const availablePackages = useMemo(() => {
    const tailoredPackage: Package = custom ?? { name: "Tailored support", label: "YOUR NEEDS", window: "Designed around your circumstances", price: "Investment shared privately", description: "A flexible solution shaped around the level of guidance, time, and support you need.", features: ["A package shaped around your priorities", "Clear support at every stage", "A private conversation to agree the right scope"], accent: "#a88ae6" };
    return [...(selected.safar ? [{ ...safar, price: packagePrices.safar }] : []), ...(selected.amanah ? [{ ...amanah, price: packagePrices.amanah }] : []), tailoredPackage];
  }, [custom, packagePrices, selected]);
  const slides = useMemo(() => [<TitleSlide key="welcome" greeting={greeting} />, <WhySlide key="why" />, <OutcomeSlide key="outcome" />, <ExperienceSlide key="experience" />, <ProcessSlide key="process" />, ...(availablePackages.length ? [<PackageSelectorSlide key="packages" items={availablePackages} pricesVisible={showPrices} />] : []), <ReassuranceSlide key="reassurance" />, <CloseSlide key="close" />], [availablePackages, greeting, showPrices]);
  const go = useCallback((next: number) => setSlide(Math.min(Math.max(next, 0), slides.length - 1)), [slides.length]);
  const toggleFullscreen = useCallback(async () => { if (!document.fullscreenElement) await deckRef.current?.requestFullscreen(); else await document.exitFullscreen(); }, []);
  useEffect(() => { const onKey = (event: KeyboardEvent) => { if (["ArrowRight", "ArrowDown", " ", "Enter"].includes(event.key)) { event.preventDefault(); go(slide + 1); } if (["ArrowLeft", "ArrowUp"].includes(event.key)) { event.preventDefault(); go(slide - 1); } if (event.key === "f" || event.key === "F") void toggleFullscreen(); if (event.key === "Escape" && document.fullscreenElement) void document.exitFullscreen(); }; const onFullscreen = () => setIsFullscreen(Boolean(document.fullscreenElement)); window.addEventListener("keydown", onKey); document.addEventListener("fullscreenchange", onFullscreen); return () => { window.removeEventListener("keydown", onKey); document.removeEventListener("fullscreenchange", onFullscreen); }; }, [go, slide, toggleFullscreen]);
  useEffect(() => setSlide((current) => Math.min(current, slides.length - 1)), [slides.length]);
  return <div ref={deckRef} className="relative h-[100svh] min-h-[620px] overflow-hidden bg-[#101d3b] font-sans text-white" onTouchStart={(e) => { touchStart.current = e.changedTouches[0].clientX; }} onTouchEnd={(e) => { if (touchStart.current === null) return; const distance = e.changedTouches[0].clientX - touchStart.current; if (Math.abs(distance) > 50) go(slide + (distance < 0 ? 1 : -1)); touchStart.current = null; }}><div className="absolute inset-0 bg-[radial-gradient(circle_at_12%_12%,rgba(216,91,135,.26),transparent_25%),radial-gradient(circle_at_85%_82%,rgba(214,172,87,.2),transparent_26%)]" /><div className="relative flex h-full flex-col"><header className="z-20 flex items-center justify-between px-6 py-5 sm:px-10"><Image src="/jodohmu-logo.png" alt="Jodohmu" width={45} height={45} className="h-10 w-10 object-contain brightness-0 invert" priority /><p className="hidden text-xs font-bold uppercase tracking-[.24em] text-white/55 sm:block">Jodohmu · International matchmaking</p><div className="flex gap-2">{presenter && <button onClick={() => setSettingsOpen(true)} className="inline-flex items-center gap-2 rounded-full bg-[#e6bd69] px-3 py-2 text-xs font-bold text-[#17213d]"><Settings2 className="h-4 w-4" /><span className="hidden sm:inline">Presenter tools</span></button>}<button onClick={() => void toggleFullscreen()} className="inline-flex items-center gap-2 rounded-full border border-white/20 px-3 py-2 text-xs font-semibold text-white/75 transition hover:bg-white/10 hover:text-white" aria-label="Toggle full screen"><Expand className="h-4 w-4" /><span className="hidden sm:inline">{isFullscreen ? "Exit full screen" : "Full screen"}</span></button></div></header><div className="relative flex flex-1 items-center overflow-hidden"><div className="flex h-full w-full transition-transform duration-700 ease-[cubic-bezier(.22,1,.36,1)]" style={{ transform: `translateX(-${slide * 100}%)` }}>{slides.map((content, index) => <section key={index} aria-hidden={slide !== index} className="flex h-full w-full shrink-0 items-center justify-center px-7 pb-20 pt-2 sm:px-16 lg:px-24">{content}</section>)}</div></div><footer className="z-20 flex items-center justify-between px-6 pb-6 sm:px-10 sm:pb-8"><div className="flex gap-2">{slides.map((_, index) => <button aria-label={`Go to slide ${index + 1}`} onClick={() => go(index)} key={index} className={`h-1.5 rounded-full transition-all ${index === slide ? "w-8 bg-[#e6bd69]" : "w-1.5 bg-white/30 hover:bg-white/60"}`} />)}</div><p className="hidden text-xs text-white/45 sm:block">Use ← → keys or swipe to navigate</p><div className="flex gap-2"><button onClick={() => go(slide - 1)} disabled={slide === 0} className="grid h-10 w-10 place-items-center rounded-full border border-white/20 text-white transition hover:bg-white/10 disabled:cursor-not-allowed disabled:opacity-30" aria-label="Previous slide"><ArrowLeft className="h-4 w-4" /></button><button onClick={() => go(slide + 1)} disabled={slide === slides.length - 1} className="grid h-10 w-10 place-items-center rounded-full bg-[#d85b87] text-white transition hover:bg-[#c44874] disabled:cursor-not-allowed disabled:opacity-40" aria-label="Next slide"><ArrowRight className="h-4 w-4" /></button></div></footer></div>{settingsOpen && <PresenterTools selected={selected} setSelected={setSelected} showPrices={showPrices} setShowPrices={setShowPrices} packagePrices={packagePrices} setPackagePrices={setPackagePrices} custom={custom} setCustom={setCustom} onClose={() => setSettingsOpen(false)} />}</div>;
}

function TitleSlide({ greeting }: { greeting: string }) { return <div className="mx-auto max-w-5xl text-center"><p className="text-xs font-bold uppercase tracking-[.28em] text-[#e6bd69]">{greeting}</p><h1 className="mx-auto mt-8 max-w-4xl font-serif text-5xl font-bold leading-[1.05] tracking-tight sm:text-7xl lg:text-8xl">A meaningful path to meet an <span className="italic text-[#ef91b1]">Indonesian spouse.</span></h1><p className="mx-auto mt-8 max-w-2xl text-lg leading-8 text-white/70 sm:text-xl">For people who are serious about marriage — guided with cultural care, human judgment, and genuine intention.</p><div className="mt-12 flex justify-center"><ChevronDown className="h-6 w-6 animate-bounce text-[#e6bd69]" /></div></div>; }
function WhySlide() { const items = [[ShieldCheck,"Verified and protected","Every introduction begins with careful verification and serious intent."],[Users,"Human-led matchmaking","A real matchmaker understands your values — no algorithms or endless swiping."],[Heart,"Dignified by design","Family, culture, and boundaries are respected at every stage."]]; return <div className="mx-auto w-full max-w-6xl"><p className="text-center text-xs font-bold uppercase tracking-[.25em] text-[#e6bd69]">The Jodohmu difference</p><h2 className="mt-3 text-center font-serif text-4xl font-bold sm:text-6xl">The search for a life partner deserves more care.</h2><div className="mt-7 grid gap-5 lg:grid-cols-[1.12fr_.88fr] lg:items-stretch"><div className="relative min-h-[330px] overflow-hidden rounded-3xl border border-white/15 shadow-xl shadow-black/20"><Image src="/presentations-foreign-cindo-family.png" alt="A young Chinese Indonesian family enjoying time together at home" fill className="object-cover" sizes="(max-width: 1024px) 95vw, 640px" /><div className="absolute inset-0 bg-[linear-gradient(90deg,rgba(16,29,59,.54),transparent_62%)]" /><p className="absolute bottom-6 left-6 max-w-[15rem] font-serif text-2xl font-bold leading-tight text-white">Because the goal is not a match. <span className="italic text-[#f3c5d4]">It is a life together.</span></p></div><div className="grid gap-3 sm:grid-cols-3 lg:grid-cols-1">{items.map(([Icon, title, text]) => { const ItemIcon = Icon as typeof ShieldCheck; return <div key={String(title)} className="rounded-2xl border border-white/15 bg-white/[.07] p-5 backdrop-blur"><ItemIcon className="h-7 w-7 text-[#ef91b1]" /><h3 className="mt-3 text-lg font-bold">{title as string}</h3><p className="mt-2 text-sm leading-5 text-white/65">{text as string}</p></div>; })}</div></div></div>; }
function OutcomeSlide() { return <div className="mx-auto grid w-full max-w-6xl gap-10 lg:grid-cols-[1fr_.9fr] lg:items-center"><div><p className="text-xs font-bold uppercase tracking-[.25em] text-[#e6bd69]">What changes</p><h2 className="mt-5 font-serif text-5xl font-bold leading-tight sm:text-7xl">Less guessing.<br /><span className="italic text-[#ef91b1]">More confidence.</span></h2><p className="mt-7 max-w-xl text-lg leading-8 text-white/70">Meeting the right person should not depend on luck, confusing signals, or a profile that looks good for five seconds. Jodohmu creates the clarity to make a serious decision with calm and confidence.</p></div><div className="space-y-4">{[["Your intention is understood before an introduction is made."],["You meet people who have been assessed for seriousness and readiness."],["Every step has a clear next move — with someone alongside you."]].map(([text], index) => <div key={index} className="flex gap-5 rounded-2xl border border-white/15 bg-white/[.07] p-5"><span className="font-serif text-2xl text-[#e6bd69]">0{index + 1}</span><p className="leading-7 text-white/80">{text}</p></div>)}</div></div>; }
function ProcessSlide() { const [active, setActive] = useState(0); const steps = [{ number: "01", title: "Onboarding meeting", short: "We begin by listening.", detail: "A private meeting to understand your values, hopes, boundaries, and the kind of marriage you want to build.", outcome: "You are understood before you are introduced." }, { number: "02", title: "Facilitated online meetings", short: "Connection, with structure.", detail: "Meaningful online meetings take place with a facilitator present to protect dignity and keep the conversation purposeful.", outcome: "You can explore compatibility without unnecessary uncertainty." }, { number: "03", title: "Background check", short: "Trust is earned.", detail: "We review identity, important documents, references, employment, and relevant online information with discretion.", outcome: "You meet people who have been seriously screened." }, { number: "04", title: "Joble in Indonesia", short: "When both are ready.", detail: "If the prospective partner is in Indonesia and both people wish to proceed, we arrange a thoughtful in-person Joble here.", outcome: "The next step feels safe, intentional, and real." }, { number: "05", title: "Family gathering", short: "A future includes family.", detail: "When the connection becomes serious, we create a respectful space for families to meet and understand each other.", outcome: "Important relationships are included at the right time." }, { number: "06", title: "Psychological assessment", short: "More than a profile.", detail: "A clinical psychology assessment helps us understand communication style, readiness, and compatibility beyond surface details.", outcome: "Introductions are made with greater clarity and care." }]; const current = steps[active]; return <div className="mx-auto -mt-8 w-full max-w-6xl"><p className="text-center text-xs font-bold uppercase tracking-[.25em] text-[#e6bd69]">The Jodohmu journey</p><h2 className="mt-2 text-center font-serif text-4xl font-bold leading-tight sm:text-5xl">Every step is designed to make a serious decision feel safer.</h2><p className="mx-auto mt-3 max-w-2xl text-center leading-6 text-white/65">A thoughtful path that builds confidence before two people take the next step.</p><div className="mt-7 grid grid-cols-2 gap-3 sm:grid-cols-3 lg:grid-cols-6">{steps.map((step, index) => <button key={step.number} onClick={() => setActive(index)} className={`group relative min-h-[118px] overflow-hidden rounded-2xl border p-4 text-left transition duration-300 ${active === index ? "border-[#e6bd69] bg-[linear-gradient(145deg,rgba(230,189,105,.2),rgba(255,255,255,.1))] shadow-lg shadow-black/20" : "border-white/10 bg-white/[.04] hover:-translate-y-1 hover:border-white/35 hover:bg-white/[.09]"}`}><span className={`grid h-8 w-8 place-items-center rounded-full text-xs font-bold ${active === index ? "bg-[#e6bd69] text-[#17213d]" : "bg-white/10 text-white/60"}`}>{step.number}</span><p className="mt-4 pr-4 text-sm font-bold leading-5">{step.title}</p>{index < steps.length - 1 && <ArrowRight className={`absolute bottom-4 right-3 h-4 w-4 transition ${active === index ? "text-[#e6bd69]" : "text-white/25 group-hover:translate-x-1 group-hover:text-white/60"}`} />}</button>)}</div><div className="mt-5 rounded-3xl border border-white/15 bg-white/[.08] p-6 sm:grid sm:grid-cols-[.7fr_1.3fr] sm:gap-10 sm:p-8"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-[#e6bd69]">{current.short}</p><h3 className="mt-2 font-serif text-3xl font-bold leading-tight">{current.title}</h3></div><div><p className="leading-7 text-white/75">{current.detail}</p><p className="mt-4 border-l-2 border-[#ef91b1] pl-4 text-sm font-bold leading-6 text-[#f3b2c8]">{current.outcome}</p></div></div><p className="mt-4 text-center text-xs text-white/45">Select a stage to explore the journey</p></div>; }
function ExperienceSlide() { return <div className="mx-auto w-full max-w-6xl text-center"><p className="text-xs font-bold uppercase tracking-[.25em] text-[#e6bd69]">How it feels</p><h2 className="mt-4 font-serif text-4xl font-bold leading-tight sm:text-6xl">A calm process for a life-changing decision.</h2><div className="relative mx-auto mt-8 aspect-video w-full max-w-5xl overflow-hidden rounded-[2rem] border border-white/15 shadow-2xl shadow-black/30"><Image src="/presentations-foreign-muslim-journey-centered.png" alt="A respectful facilitated conversation between a Black Muslim woman and Indonesian Muslim man" fill className="object-cover" sizes="(max-width: 1024px) 95vw, 1024px" /><div className="absolute inset-x-0 bottom-0 bg-gradient-to-t from-[#101d3b]/95 via-[#101d3b]/40 to-transparent px-7 pb-7 pt-20 sm:px-10 sm:pb-9"><p className="mx-auto max-w-xl text-base leading-7 text-white/85">Enough safety, understanding, and time for two people — and their families — to explore what a future together could genuinely look like.</p><p className="mt-3 text-xs font-bold uppercase tracking-[.18em] text-[#ef91b1]">Private · Facilitated · Respectful</p></div></div></div>; }
function PackageSelectorSlide({ items, pricesVisible }: { items: Package[]; pricesVisible: boolean }) { const [active, setActive] = useState<string | null>(null); const selectedPackage = items.find((item) => item.name === active); const isTailored = selectedPackage && selectedPackage.name !== "Safar" && selectedPackage.name !== "Amanah"; const cardTitle = (item: Package) => item.name === "Safar" ? "Getting started" : item.name === "Amanah" ? "Complete solution" : "Solution that suits you"; const cardCopy = (item: Package) => item.name === "Safar" ? "A clear, supported first step." : item.name === "Amanah" ? "Deeper guidance from start to finish." : "A journey shaped around your needs."; return <div className="mx-auto w-full max-w-6xl"><p className="text-center text-xs font-bold uppercase tracking-[.25em] text-[#e6bd69]">Your options</p><h2 className="mt-2 text-center font-serif text-4xl font-bold sm:text-5xl">Choose the support that feels right.</h2><p className="mx-auto mt-2 max-w-2xl text-center leading-6 text-white/65">Start with the type of support you need. Select one to see the details.</p><div className={`mx-auto mt-6 grid max-w-4xl gap-3 ${items.length === 1 ? "max-w-sm" : items.length === 2 ? "sm:grid-cols-2" : "sm:grid-cols-3"}`}>{items.map((item) => <button key={item.name} onClick={() => setActive((current) => current === item.name ? null : item.name)} className={`rounded-2xl border p-4 text-left transition duration-300 ${active === item.name ? "border-white bg-white/15 shadow-xl shadow-black/20" : "border-white/15 bg-white/[.06] hover:-translate-y-1 hover:border-white/40 hover:bg-white/[.1]"}`}><span className="block h-1.5 w-10 rounded-full" style={{ backgroundColor: item.accent }} /><h3 className="mt-4 text-lg font-bold leading-tight">{cardTitle(item)}</h3><p className="mt-1 text-xs leading-5 text-white/60">{cardCopy(item)}</p><p className="mt-4 text-xs font-bold uppercase tracking-[.14em] text-white/45">{active === item.name ? "Close details" : "Explore option →"}</p></button>)}</div>{selectedPackage && !isTailored && <div className="mx-auto mt-5 max-w-5xl rounded-3xl border border-white/15 bg-white/[.08] p-6 text-left sm:p-7"><div className="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div><p className="text-xs font-bold uppercase tracking-[.18em]" style={{ color: selectedPackage.accent }}>{selectedPackage.label}</p><h3 className="mt-2 font-serif text-3xl font-bold">{selectedPackage.name}</h3><p className="mt-2 max-w-2xl leading-6 text-white/70">{selectedPackage.description}</p></div>{pricesVisible && <div className="shrink-0 text-right"><p className="text-3xl font-bold" style={{ color: selectedPackage.accent }}>{selectedPackage.price}</p>{selectedPackage.savings && <p className="mt-0.5 text-xs font-bold text-white/50">{selectedPackage.savings}</p>}</div>}</div><div className="mt-5 border-t border-white/10 pt-5"><p className="text-xs font-bold uppercase tracking-[.18em] text-white/45">Your journey, step by step</p><ol className="mt-3 space-y-2.5">{selectedPackage.features.map((feature, i) => <li key={feature} className="flex items-center gap-3 text-sm leading-5 text-white/85"><span className="grid h-5 w-5 shrink-0 place-items-center rounded-full text-[11px] font-bold text-[#101d3b]" style={{ backgroundColor: selectedPackage.accent }}>{i + 1}</span>{feature}</li>)}</ol></div></div>}{isTailored && <TailoredPlanBuilder />}</div>; }

function TailoredPlanBuilder() { const options = [{ id: "onboarding", title: "Onboarding & profile", price: 200 }, { id: "psychology", title: "Psychological assessment", price: 200 }, { id: "background", title: "Background check", price: 150 }, { id: "online", title: "Online meetings", price: 150 }, { id: "family", title: "Family gathering", price: 150 }, { id: "indonesia", title: "Joble in Indonesia", price: 150 }]; const [chosen, setChosen] = useState<string[]>(["onboarding", "online"]); const [order, setOrder] = useState(options.map((option) => option.id)); const [dragging, setDragging] = useState<string | null>(null); const [onlineSessions, setOnlineSessions] = useState(3); const [psychologySessions, setPsychologySessions] = useState(1); const orderedOptions = order.map((id) => options.find((option) => option.id === id)!).filter(Boolean); const toggle = (id: string) => setChosen((current) => current.includes(id) ? current.filter((item) => item !== id) : [...current, id]); const changeCount = (setter: React.Dispatch<React.SetStateAction<number>>, amount: number) => setter((count) => Math.max(1, Math.min(12, count + amount))); const moveTo = (target: string) => { if (!dragging || dragging === target) return; setOrder((current) => { const next = current.filter((id) => id !== dragging); next.splice(next.indexOf(target), 0, dragging); return next; }); setDragging(null); }; const lineTotal = (option: { id: string; price: number }) => option.id === "online" ? option.price * onlineSessions : option.id === "psychology" ? option.price * psychologySessions : option.price; const total = orderedOptions.filter((option) => chosen.includes(option.id)).reduce((sum, option) => sum + lineTotal(option), 0); const summary = orderedOptions.filter((option) => chosen.includes(option.id)).map((option) => option.id === "online" ? `${onlineSessions} online meeting${onlineSessions === 1 ? "" : "s"}` : option.id === "psychology" ? `${psychologySessions} psychology session${psychologySessions === 1 ? "" : "s"}` : option.title); return <div className="mx-auto mt-5 max-w-5xl rounded-3xl border border-[#a88ae6]/50 bg-[#a88ae6]/[.09] p-5 text-left sm:p-6"><div className="flex items-end justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-[#c9b6ff]">Build your marriage journey</p><h3 className="mt-1 font-serif text-2xl font-bold">Choose what you need.</h3></div><p className="text-xs font-bold text-[#c9b6ff]">{chosen.length} areas selected</p></div><p className="mt-1 text-xs text-white/45">Drag the cards to arrange your ideal journey.</p><div className="mt-4 grid grid-cols-2 gap-2 sm:grid-cols-3">{orderedOptions.map((option) => { const selected = chosen.includes(option.id); const hasCounter = selected && (option.id === "online" || option.id === "psychology"); const value = option.id === "online" ? onlineSessions : psychologySessions; const setter = option.id === "online" ? setOnlineSessions : setPsychologySessions; return <div key={option.id} draggable onDragStart={() => setDragging(option.id)} onDragEnd={() => setDragging(null)} onDragOver={(event) => event.preventDefault()} onDrop={() => moveTo(option.id)} className={`relative rounded-xl border transition ${dragging === option.id ? "scale-95 opacity-50" : ""} ${selected ? "border-[#c9b6ff] bg-[#a88ae6]/25" : "border-white/10 bg-white/[.04] hover:border-white/35"}`}><span className="absolute right-2 top-2 cursor-grab text-xs tracking-[-.18em] text-white/35" aria-hidden>⋮⋮</span><button onClick={() => toggle(option.id)} className="flex min-h-[54px] w-full items-center gap-3 px-3 py-2 pr-8 text-left"><span className={`grid h-5 w-5 shrink-0 place-items-center rounded-full text-xs font-bold ${selected ? "bg-[#c9b6ff] text-[#211a3b]" : "bg-white/10 text-white/45"}`}>{selected ? "✓" : "+"}</span><span><p className="text-xs font-bold leading-4">{option.title}</p><p className="mt-0.5 text-[11px] font-bold text-[#c9b6ff]">${option.price}{option.id === "online" || option.id === "psychology" ? " each" : ""}</p></span></button>{hasCounter && <div className="flex items-center justify-between border-t border-[#c9b6ff]/25 px-3 py-2"><span className="text-[10px] font-bold uppercase tracking-[.12em] text-[#c9b6ff]">How many?</span><div className="flex items-center gap-2"><button onClick={() => changeCount(setter, -1)} className="grid h-6 w-6 place-items-center rounded-full bg-white/10 text-sm font-bold hover:bg-white/20" aria-label={`Reduce ${option.title}`}>−</button><span className="w-4 text-center text-sm font-bold text-[#c9b6ff]">{value}</span><button onClick={() => changeCount(setter, 1)} className="grid h-6 w-6 place-items-center rounded-full bg-[#c9b6ff] text-sm font-bold text-[#211a3b]" aria-label={`Increase ${option.title}`}>+</button></div></div>}</div>; })}</div><div className="mt-3 rounded-xl bg-[#101d3b]/70 px-4 py-3"><div className="flex items-center justify-between"><p className="text-xs font-bold uppercase tracking-[.16em] text-white/45">Your tailored journey</p>{chosen.length > 0 && <p className="text-sm font-bold text-[#c9b6ff]">${total}</p>}</div><p className="mt-1 text-sm leading-5 text-white/80">{summary.length ? summary.join(" → ") : "Choose the support you would like to include."}</p></div></div>; }
function ReassuranceSlide() { const answers = [["Do I need to speak Bahasa Indonesia?","No. We bridge the language and cultural gaps throughout the journey."],["Do I need to visit Indonesia first?","No. The in-person visit happens only after both people wish to move forward."],["Can I pay in installments?","Yes. You can begin with a minimum 20% payment, then we can agree a clear payment plan together."],["Is there a refund?","Yes. It depends on the stage you are at in the journey — our team coordinates this with you directly."],["Can I change the package?","Yes. We can revisit the package and adjust the way we work as the situation, priorities, and level of support become clearer."],["Can I match with another foreign national — not only an Indonesian?","Yes, we can explore this. The available pool may be more limited because family involvement, verification, and assessment need to be managed thoughtfully across countries."],["What if I have questions before deciding?","That is exactly what the first private conversation is for — with no pressure to commit."]]; const [active, setActive] = useState(0); const current = answers[active]; const colors = ["#e6bd69","#ef91b1","#7ed6c1","#c9b6ff","#ef91b1","#7ed6c1","#e6bd69"]; return <div className="mx-auto w-full max-w-6xl"><p className="text-center text-xs font-bold uppercase tracking-[.25em] text-[#e6bd69]">Before you decide</p><h2 className="mt-3 text-center font-serif text-4xl font-bold sm:text-6xl">The important questions, answered.</h2><p className="mx-auto mt-3 max-w-xl text-center text-sm leading-6 text-white/60">Choose a card to uncover the answer.</p><div className="mt-7 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{answers.map(([question], index) => { const color = colors[index]; const open = active === index; return <button key={question} onClick={() => setActive(index)} style={{ borderColor: open ? color : undefined, background: open ? `linear-gradient(135deg,${color}33,rgba(255,255,255,.08))` : undefined }} className={`group relative min-h-[116px] overflow-hidden rounded-2xl border p-4 text-left transition duration-300 ${open ? "-translate-y-1 shadow-xl shadow-black/25" : "border-white/15 bg-white/[.06] hover:-translate-y-1 hover:border-white/40 hover:bg-white/[.11]"}`}><span className="absolute -right-5 -top-7 h-20 w-20 rounded-full opacity-20 blur-sm" style={{ backgroundColor: color }} /><div className="relative flex items-start justify-between"><span className="grid h-8 w-8 place-items-center rounded-full text-xs font-black text-[#17213d]" style={{ backgroundColor: color }}>0{index + 1}</span><span className={`grid h-7 w-7 place-items-center rounded-full border text-base font-light transition ${open ? "border-white/50 text-white" : "border-white/20 text-white/55 group-hover:bg-white/10"}`}>{open ? "−" : "+"}</span></div><h3 className="relative mt-4 pr-4 text-sm font-bold leading-5">{question}</h3></button>; })}</div><div className="mt-4 overflow-hidden rounded-3xl border border-white/15 bg-white/[.08] sm:grid sm:grid-cols-[.85fr_1.15fr]"><div className="p-6 sm:p-8" style={{ background: `linear-gradient(135deg,${colors[active]}30,transparent)` }}><p className="text-xs font-bold uppercase tracking-[.2em]" style={{ color: colors[active] }}>Your question</p><h3 className="mt-3 font-serif text-2xl font-bold leading-tight sm:text-3xl">{current[0]}</h3></div><div className="flex items-center p-6 sm:p-8"><p className="leading-7 text-white/80">{current[1]}</p></div></div></div>; }
function CloseSlide() { return <div className="relative mx-auto w-full max-w-6xl overflow-hidden rounded-[2rem] border border-white/15 shadow-2xl shadow-black/30"><Image src="/presentations-foreign-wedding-celebration.png" alt="An Indonesian Muslim bride and her foreign husband celebrating their wedding with family" fill className="object-cover" sizes="(max-width: 1024px) 95vw, 1152px" priority /><div className="relative min-h-[500px] bg-[linear-gradient(90deg,rgba(16,29,59,.94)_0%,rgba(16,29,59,.78)_42%,rgba(16,29,59,.12)_100%)] p-8 sm:min-h-[540px] sm:p-12"><div className="flex h-full max-w-xl flex-col justify-center text-left"><p className="text-xs font-bold uppercase tracking-[.25em] text-[#e6bd69]">A thoughtful beginning</p><h2 className="mt-6 font-serif text-4xl font-bold leading-tight sm:text-6xl">A future worth building <span className="italic text-[#ef91b1]">with care.</span></h2><p className="mt-6 max-w-lg text-base leading-7 text-white/80 sm:text-lg sm:leading-8">Your first conversation is simply a chance to understand the journey. We will listen, answer your questions, and help you decide whether Jodohmu is the right fit.</p><a href={whatsappHref} target="_blank" rel="noopener noreferrer" className="mt-9 inline-flex w-fit items-center gap-2 rounded-full bg-[#d85b87] px-7 py-4 font-bold transition hover:bg-[#c44874]"><MessageCircle className="h-5 w-5" />Start a private conversation</a></div></div></div>; }

function PresenterTools({ selected, setSelected, showPrices, setShowPrices, packagePrices, setPackagePrices, custom, setCustom, onClose }: { selected: { safar: boolean; amanah: boolean }; setSelected: React.Dispatch<React.SetStateAction<{ safar: boolean; amanah: boolean }>>; showPrices: boolean; setShowPrices: React.Dispatch<React.SetStateAction<boolean>>; packagePrices: { safar: string; amanah: string }; setPackagePrices: React.Dispatch<React.SetStateAction<{ safar: string; amanah: string }>>; custom: Package | null; setCustom: React.Dispatch<React.SetStateAction<Package | null>>; onClose: () => void }) { const [name, setName] = useState(custom?.name ?? ""); const [price, setPrice] = useState(custom?.price ?? ""); const [window, setWindow] = useState(custom?.window ?? ""); const [description, setDescription] = useState(custom?.description ?? ""); const [features, setFeatures] = useState(custom?.features.join("\n") ?? ""); const save = () => { if (!name.trim()) return; setCustom({ name: name.trim(), label: "TAILORED FOR YOU", price: price.trim() || "Investment shared privately", window: window.trim() || "A tailored journey", description: description.trim() || "A tailored level of support, built around your circumstances.", features: features.split("\n").map((item) => item.trim()).filter(Boolean), accent: "#a88ae6" }); }; return <aside className="absolute inset-y-0 right-0 z-50 w-full max-w-md overflow-y-auto border-l border-white/10 bg-[#17213d] p-6 shadow-2xl sm:p-8"><div className="flex items-start justify-between"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-[#e6bd69]">Presenter mode</p><h2 className="mt-2 font-serif text-3xl font-bold">Tailor this deck</h2></div><button onClick={onClose} className="rounded-full p-2 hover:bg-white/10" aria-label="Close presenter tools"><X className="h-5 w-5" /></button></div><p className="mt-4 text-sm leading-6 text-white/65">Choose packages, change their prices, or build one live during the conversation.</p><div className="mt-8 space-y-3"><div className="flex items-center justify-between"><p className="text-xs font-bold uppercase tracking-[.18em] text-white/45">Packages and prices</p><label className="flex items-center gap-2 text-xs font-bold text-[#ef91b1]">Show prices <input type="checkbox" checked={showPrices} onChange={(e) => setShowPrices(e.target.checked)} className="h-4 w-4 accent-[#d85b87]" /></label></div>{([['safar', 'Safar'], ['amanah', 'Amanah']] as const).map(([key, label]) => <div key={key} className="rounded-xl border border-white/15 p-4"><div className="flex items-center justify-between"><span className="font-bold">{label}</span><label className="flex items-center gap-2 text-xs text-white/65">Show <input type="checkbox" checked={selected[key]} onChange={(e) => setSelected((current) => ({ ...current, [key]: e.target.checked }))} className="h-4 w-4 accent-[#d85b87]" /></label></div><input value={packagePrices[key]} onChange={(e) => setPackagePrices((current) => ({ ...current, [key]: e.target.value }))} placeholder="Any price, e.g. $1,850" className="mt-3 w-full rounded-lg border border-white/15 bg-white/[.06] px-3 py-2.5 text-sm outline-none placeholder:text-white/35 focus:border-[#e6bd69]" /></div>)}</div><div className="mt-8 border-t border-white/10 pt-7"><div className="flex items-center justify-between"><p className="text-xs font-bold uppercase tracking-[.18em] text-white/45">Custom package</p>{custom && <button onClick={() => setCustom(null)} className="text-xs font-bold text-[#ef91b1]">Remove</button>}</div><div className="mt-4 space-y-3"><input value={name} onChange={(e) => setName(e.target.value)} placeholder="Package name" className="w-full rounded-xl border border-white/15 bg-white/[.06] px-4 py-3 text-sm outline-none placeholder:text-white/35 focus:border-[#e6bd69]" /><div className="grid grid-cols-2 gap-3"><input value={price} onChange={(e) => setPrice(e.target.value)} placeholder="Any price" className="w-full rounded-xl border border-white/15 bg-white/[.06] px-4 py-3 text-sm outline-none placeholder:text-white/35 focus:border-[#e6bd69]" /><input value={window} onChange={(e) => setWindow(e.target.value)} placeholder="Duration" className="w-full rounded-xl border border-white/15 bg-white/[.06] px-4 py-3 text-sm outline-none placeholder:text-white/35 focus:border-[#e6bd69]" /></div><textarea value={description} onChange={(e) => setDescription(e.target.value)} placeholder="Short value statement" rows={3} className="w-full rounded-xl border border-white/15 bg-white/[.06] px-4 py-3 text-sm outline-none placeholder:text-white/35 focus:border-[#e6bd69]" /><textarea value={features} onChange={(e) => setFeatures(e.target.value)} placeholder={"Features — one per line"} rows={5} className="w-full rounded-xl border border-white/15 bg-white/[.06] px-4 py-3 text-sm outline-none placeholder:text-white/35 focus:border-[#e6bd69]" /><button onClick={save} className="w-full rounded-xl bg-[#a88ae6] px-4 py-3 text-sm font-bold text-[#171129]">{custom ? "Update custom package" : "Add custom package"}</button></div></div></aside>; }
